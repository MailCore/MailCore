{
    IBClasses = (
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }, 
        {
            ACTIONS = {connect = id; }; 
            CLASS = MyController; 
            LANGUAGE = ObjC; 
            OUTLETS = {password = id; port = id; server = id; useTLS = id; username = id; }; 
            SUPERCLASS = NSObject; 
        }
    ); 
    IBVersion = 1; 
}